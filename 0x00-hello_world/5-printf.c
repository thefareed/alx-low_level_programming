#include <stdio.h>
/**
 * main - A program that prints exactly with proper grammar but the outcome is ia piece of art followed by a new line
 * Return: 0(Sucess)
 */
int main(void)
{
printf("\"with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
